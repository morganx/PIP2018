Files in this directory:

#Derived from original files, with all samples >=1.5 IQR of E.coli removed (IQRs calculated by timepoint)

filtered-taxonomy.tsv
filtered-modules.tsv
filtered-pathways.tsv

#Values in this table are calculated by multiplying relative abundances in taxonomy.tsv by the total numbers of reads in the corresponding sample sequencing library 

NZGL_taxonomy_counts_table.csv

#Same as above, but biom formatted
NZGL_taxonomy_SP_counts.biom

# Shows total reads in each fastq file, mapped by patient, library, and time.
Reshaped_NZGL_sum_reads_id.csv

# Metadata file for SRA upload
SRA_metadata_PIP.csv



